package com.config;

/**
 * Legacy CacheConfig - Content merged into LedgerRedisConfig to resolve bean conflicts.
 * Removed @Configuration to prevent bean definition conflicts with LedgerRedisConfig.
 */
public class CacheConfig {
    // Bean definitions moved to LedgerRedisConfig.java
}
