package com.cdc.config;

// eosConnectionManager bean moved to UnifiedCdcRunnerConfig so its lifecycle
// is tied to CDC leadership rather than Spring context startup.
// This file is intentionally empty — kept to avoid stale import errors in IDEs
// that may have cached references to this class.
